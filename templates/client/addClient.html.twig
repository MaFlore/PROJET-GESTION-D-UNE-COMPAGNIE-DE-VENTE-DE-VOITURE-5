
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}

		form {

			margin-top: 100px;
		}
	
	</style>

	<div class="example-wrapper">
        <a style="float:left" href="/client" class="btn btn-warning">Liste des clients</a>
		{{ form_start(form) }}
		<h2 style="text-align:center;">Ajouter un client</h2>
	</br>
		{{ form_row(form.cni) }}
		{{ form_row(form.nom) }}
		{{ form_row(form.prenom) }}
        {{ form_row(form.telephone) }}
        {{ form_row(form.nomUtilisateur) }}
        {{ form_row(form.password) }}
		<br>
		<button type="submit" class="btn btn-primary">Ajouter</button> <button type="reset" class="btn btn-danger">Annuler</button>
	
		{{ form_end(form) }}
	</div>
